# Added by code-review-graph
.code-review-graph/
.local

# opencode runtime (plugins/skills cache, not project config)
.agent/

# cross-compile build artifacts
wardenssh
wardenssh.exe
wardenssh-*

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
